# La Suite Meet - Configuration PostgreSQL
# Copiez ce fichier vers .env.postgresql et remplissez les valeurs

# === DATABASE CONFIGURATION ===
DB_HOST=postgresql
DB_NAME=meet
DB_USER=meet
# Générer avec: openssl rand -base64 32
DB_PASSWORD=CHANGEME_GENERATE_SECURE_PASSWORD
DB_PORT=5432

# === POSTGRESQL CONTAINER CONFIG ===
POSTGRES_DB=meet
POSTGRES_USER=meet
POSTGRES_PASSWORD=${DB_PASSWORD}
